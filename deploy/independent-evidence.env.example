# Optional candidate-only rollout. Do not turn on from a model tool/RPC request.
# Leave the current Python Luna command, gpt-5.6-luna / low, budgets and thresholds unchanged.
EIMEMORY_INDEPENDENT_EVIDENCE_MODE=off
# off: original verifier. shadow: local observation + original verifier.
# enforce: reviewed current contract may avoid the model; unknown/stale -> original verifier.
# This optional negative shortcut requires independent acceptance; it is NOT enabled by enforce.
EIMEMORY_RECALL_ATTRIBUTE_PRECHECK=0
# Never add OpenClaw pooling/keepalive merely to activate this architecture.
